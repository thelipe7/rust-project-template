# The system packages this project's native dependencies link against, read by
# `.github/actions/rust` before every Linux job builds anything. Names as apt
# knows them; whitespace and newlines both separate, `#` starts a comment.
#
# This file exists because an answer said the project needs them. Adding one
# later is an edit here and nowhere else — no workflow names them.
{{ system_packages }}
